// ignore_for_file: prefer_const_constructors
/*
import 'package:flutter_test/flutter_test.dart';
import 'package:frog_chat/group/bloc/bloc.dart';

void main() {
  group('GroupState', () {
    test('supports value equality', () {
      expect(
        GroupState(),
        equals(
          const GroupState(),
        ),
      );
    });

    group('constructor', () {
      test('can be instantiated', () {
        expect(
          const GroupState(),
          isNotNull,
        );
      });
    });

    group('copyWith', () {
      test(
        'copies correctly '
        'when no argument specified',
        () {
          const groupState = GroupState(
            customProperty: 'My property',
          );
          expect(
            groupState.copyWith(),
            equals(groupState),
          );
        },
      );

      test(
        'copies correctly '
        'when all arguments specified',
        () {
          const groupState = GroupState(
            customProperty: 'My property',
          );
          final otherGroupState = GroupState(
            customProperty: 'My property 2',
          );
          expect(groupState, isNot(equals(otherGroupState)));

          expect(
            groupState.copyWith(
              customProperty: otherGroupState.customProperty,
            ),
            equals(otherGroupState),
          );
        },
      );
    });
  });
}
*/