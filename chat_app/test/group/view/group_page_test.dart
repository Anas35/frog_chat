// ignore_for_file: prefer_const_constructors
/*
import 'package:flutter/material.dart';
import 'package:chat_app/group/group.dart';
import 'package:flutter_test/flutter_test.dart';

void main() {
  group('GroupPage', () {
    group('route', () {
      test('is routable', () {
        expect(GroupPage.route(), isA<MaterialPageRoute>());
      });
    });

    testWidgets('renders GroupView', (tester) async {
      await tester.pumpWidget(MaterialApp(home: GroupPage()));
      expect(find.byType(GroupView), findsOneWidget);
    });
  });
}
*/